<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.avneet.project">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view_main" />
        <activity android:name=".Register" />
        <activity android:name=".View_Users" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".Credential" />
        <activity android:name=".User" />
        <activity android:name=".Delete" />
        <activity android:name=".UserBackend" />
        <activity android:name=".View_Property" />
        <activity android:name=".View_prop_det" />
        <activity android:name=".UserMenu" />
        <activity android:name=".EditUser" />
        <activity android:name=".ViewDetail"></activity>
    </application>

</manifest>